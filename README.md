# Shidoku

Personal Visual Intelligence camera app — point, tap Ask, get an answer.
One HTML file (`index.html`), a stdlib Python relay (`server.py`), and a
Capacitor shell for iOS built by GitHub Actions (see `NATIVE.md`).
